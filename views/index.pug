extends layout
include ../partials/mixins

block content
  h1= title
  #user
    if user
      +navbar-mixin
      h4 Welcome #{user.username}
      a(href=user.url) Your messages
    else 
      h4 Please Sign in 
      a(href="/user/sign-in") Sign in
      h4 Don't have an account?
      a(href="/user/sign-up") Sign up